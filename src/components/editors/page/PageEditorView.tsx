import * as React from "react";
import { observer } from "mobx-react";

@observer
export class PageEditorView extends React.Component<any, any> {
    render() {
        return <div>Page Editor</div>
    }
}